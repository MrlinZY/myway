#include <stdio.h>
int main() {
    char ch;
    while ((ch=getchar()) !='\n') {
        /* code */
        printf("copythat ");
    }
    while(1);
    return 0;
}
//程序会停止，等待输入
//输入abc 输出copy copy copy